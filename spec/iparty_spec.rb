# frozen_string_literal: true

RSpec.describe IParty do
  it "has a version number" do
    expect(IParty::VERSION).not_to be_nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
